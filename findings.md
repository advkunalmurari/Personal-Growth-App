# Findings Log

*Use this file to document new discoveries: API constraints, gotchas, useful patterns.*

- Next.js App Router with Server Components will be used.
- Supabase Row-Level Security (RLS) is required for all tables.
- UI state will primarily rely on optimistic updates to ensure zero perceived loading latency.
